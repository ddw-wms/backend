# ✅ SECURITY - Scripts Directory

## 🔒 Status: SECURED (Updated January 15, 2026)

All scripts have been updated to use environment variables instead of hardcoded credentials.

## 📋 Scripts Using Environment Variables (✅ Safe)

All scripts now read from `DATABASE_URL` in `.env` file:
- `check-dashboard-permissions.js` ✅
- `disable-all-picker-permissions.js` ✅
- `enable-all-picker-permissions.js` ✅
- `find_missing_permissions.js` ✅
- `fix-dashboard-filter-permission.js` ✅
- `list-picker-users.js` ✅
- `prune_unused_permissions.js` ✅
- `run-actual-permissions-migration.js` ✅
- `run-granular-permissions-migration.js` ✅
- `run-inbound-optimization.js` ✅
- `run-remaining-permissions-migration.js` ✅
- `test-final-perf.js` ✅
- `test-picker-complete.js` ✅
- `test-picker-login.js` ✅
- `test-real-performance.js` ✅
- `test-warm-pool.js` ✅
- `toggle-picker-permissions.js` ✅
- `toggle-picker-permissions-test.js` ✅
- `verify-final-permissions.js` ✅

## 🔧 How to Run Scripts

1. Ensure `.env` file exists with `DATABASE_URL`:
```bash
# .env
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
```

2. Run any script:
```bash
node scripts/check-dashboard-permissions.js
```

3. For test scripts that need credentials:
```bash
# Set environment variables
TEST_PICKER_USERNAME=xxx TEST_PICKER_PASSWORD=xxx node scripts/test-picker-complete.js
ADMIN_USERNAME=xxx ADMIN_PASSWORD=xxx node scripts/toggle-picker-permissions-test.js
```

## ⚠️ Important Reminders

1. **Never commit `.env` files** - they contain secrets
2. **Rotate passwords** if they were previously exposed in git history
3. **Use `.env.example`** as a template for new environments

---
**Last Updated:** January 15, 2026
**Security Status:** ✅ COMPLIANT
